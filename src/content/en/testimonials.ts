import type { TestimonialContent } from "../types";

// Intentionally empty — see src/content/fr/testimonials.ts. Never fill this
// with invented quotes; hide the related section on the page until real
// testimonials are gathered.
export const testimonials: TestimonialContent[] = [];
